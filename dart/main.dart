void main() {
  // int , double , num
  // round, ceil, floor
  // abs,toStringAsFixed
  // toInt, toDouble, toString (Numbers)

  // -- String
  // parse (String)
  // length 1,2,3,4,5 , index 0,1,2,3,4
  // isEmpty, isNotEmpty
  // contains, startsWith, endsWith
  // trim
  

  // ahmed => Ahmed  use "substring and to upperCase"
  String name = "ahmed";
  name = name[0].toUpperCase() + name.substring(1);
  print(name);
}
